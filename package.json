{
  "name": "@asp-sail/ftr-eval-mcp",
  "version": "1.0.0",
  "description": "MCP server exposing FTR (Foundational Technical Review) evaluation tools for AI assistants",
  "main": "dist/server.js",
  "bin": {
    "ftr-eval-mcp": "./dist/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist/",
    "assets/"
  ],
  "scripts": {
    "build": "tsc && npm run copy-assets && chmod +x dist/server.js",
    "copy-assets": "cp -r src/assets dist/assets",
    "start": "node dist/server.js",
    "test": "vitest --run",
    "test:watch": "vitest",
    "build:binaries": "npm run build && pkg . --out-path binaries/"
  },
  "keywords": [
    "mcp",
    "ftr",
    "aws",
    "evaluation",
    "bedrock"
  ],
  "license": "UNLICENSED",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "pdf-parse": "^1.1.1",
    "@aws-sdk/client-bedrock-runtime": "^3.0.0",
    "@aws-sdk/client-sts": "^3.0.0",
    "commander": "^12.0.0",
    "inquirer": "^9.0.0",
    "chalk": "^5.3.0",
    "ora": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0",
    "@types/node": "^20.0.0",
    "@types/inquirer": "^9.0.0",
    "pkg": "^5.8.0"
  },
  "pkg": {
    "targets": [
      "node18-macos-arm64",
      "node18-macos-x64",
      "node18-linux-x64",
      "node18-linux-arm64",
      "node18-win-x64"
    ],
    "assets": [
      "dist/assets/**/*"
    ],
    "outputPath": "binaries"
  }
}
